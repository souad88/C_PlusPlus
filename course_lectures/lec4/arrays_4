//array_name.at(index)=new_val; //you can access an element in the array with (.at(index))
#include<iostream>
#include<array>
using namespace std;
int main()
{
    array<int,4> num={1,2,3,4};
    num.at(2)=99;
    for(int i=0; i<4;i++)
    {
            cout<<num[i]<<endl;
    }
    return 0;
}
